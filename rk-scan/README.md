# rk-scan

A lightweight and efficient TCP port scanner

## Usage
```bash
rk-scan -t <ip> -d           # Default scan (1-100)
rk-scan -t <ip> -s           # Special services scan
rk-scan -t <ip> -c <port>    # Custom port scan
```
